import { css } from 'lit-element';

// language=CSS
export const styles =
    css`
        :host {
            display: grid;
            align-content: center;
        }
       
        @media (max-width: 650px) {

        }
    `;
